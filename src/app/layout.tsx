import type { Metadata } from "next";
import { Halant } from "next/font/google";
import { Inter } from "next/font/google";
import "./globals.css";
import "@/lib/gsap-setup";
import { ServiceWrapper } from "@/components/ServiceWrapper";
import Tag from "@/tag/Tag";
import { getVisualEditScript } from "@/utils/visual-edit-script";
import { Mulish } from "next/font/google";



export const metadata: Metadata = {
  title: 'EduStream LMS | Professional Online Learning',
  description: 'EduStream is a feature-rich Learning Management System for efficient course management and skill development.',
  keywords: ["LMS, e-learning, online courses, certification, react, nodejs"],
  openGraph: {
    "title": "EduStream LMS",
    "description": "Professional LMS platform for students and educators.",
    "type": "website"
  },
};

const mulish = Mulish({ variable: "--font-mulish", subsets: ["latin"] });

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <ServiceWrapper>
        <body className={`${mulish.variable} antialiased`}>
          <Tag />
          {children}
          <script
              dangerouslySetInnerHTML={{
                  __html: `${getVisualEditScript()}`
              }}
          />
        </body>
      </ServiceWrapper>
    </html>
  );
}
